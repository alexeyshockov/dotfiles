# Actually there is no need for pyenv global init:
#  - global (system) Python is managed by Homebrew
#  - per project Python is manager by direnv
#has jenv && status is-interactive && pyenv init - | source
